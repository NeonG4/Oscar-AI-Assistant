{
  "name": "oscar",
  "version": "1.0.0",
  "private": true,
  "description": "Voice-driven AI agent for iOS Shortcuts. Speak a question, get an answer as a notification.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "check": "for f in api/*.js lib/*.js public/app.js server.js test/*.js; do node --check \"$f\" || exit 1; done",
    "test": "node test/smoke.js"
  },
  "keywords": [
    "ios",
    "shortcuts",
    "siri",
    "openai",
    "vercel"
  ],
  "license": "MIT"
}
